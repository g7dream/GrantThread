# Time Illusion hosting setup

Verified setup on **14 September 2026**:

- Frontend and Cognito callback/logout URL: `https://timeillusion.com/grantthread/`
- API and S3 allowed origin: `https://timeillusion.com`; Vite base `/grantthread/`.
- Application stack `grantthread-demo`: created successfully and subsequently updated, Stockholm `eu-north-1`, `CapacityMode=shared-demo`.
- Regional Bedrock model: `amazon.nova-lite-v1:0`; direct Converse and genuine Strands reconciliation both verified.
- Separate `grantthread-demo-budget` stack: `CREATE_COMPLETE` in `us-east-1`; USD 25 monthly, 80% actual and 100% forecast alerts, credits/refunds excluded. The recipient is verified and remains private. Alerts are not a hard spending cap.

The current setup used the existing temporary root-console CLI/SDK login. No access keys or non-root development operator were created. Runtime uses Lambda IAM roles. Credentials, the budget recipient and private financial files remain outside public artifacts. The Cognito domain and other public frontend settings are visible in the compiled assets.

The current cloud-configured package is `artifacts/GrantThread-cpanel.zip`, 175,032 bytes, SHA-256 `a9f12b3001bb9fb7354048a2f7612ed93e7b6c05acc26b20d275c8223a2e928a`. Its payload bytes match the uploaded build; the later package changes deterministic file timestamps only. An initial Brotli cache retained the earlier HTML despite matching raw/gzip checks. Resaving the exact live index and manifest bytes with fresh modification times cleared it. All 16 final checks passed at 15:40 UTC: five payload hashes under identity/gzip/Brotli requests and the canonical page with obsolete validators. The actual canonical browser then loaded `index-BSFSo7GJ.js`. Evidence is `artifacts/final-hosted-encoding-verification.json`. The package now uses content-derived file timestamps; verify every supported encoding and actual browser assets after future releases. Preserve unrelated Time Illusion files.

**Verified hosted path:** actual Cognito sign-in and normal reload enter Bright Path Lab. Financial XLSX readback confirms CAD 20 actuals and CAD 250 receipts; the entered receipt basis remains CAD 1 = RON 4. The repaired backend produced report PDF version 2 with those values and a valid JSON manifest before the final frontend reload. Youth Skills version 1 was shared with Northstar and exactly one selected attachment (`invoice-insurance`). Northstar signed in and saw the EUR 400 package. With the final JS loaded, the selected original downloaded as verified 143-byte invoice text. The source-linked question, grantee answer and funder acknowledgement persisted as resolved after fresh reload. The recipient shared PDF (EUR 400) and valid shared manifest were also downloaded with the final JS. These checks and their distinct timing are recorded in `artifacts/hosted-browser-verification.json` and [EVALUATION.md](EVALUATION.md).

The public health endpoint returned 200 in AWS mode; anonymous session returned 401. Another 49 cloud application checks passed through administrative Lambda invocation with trusted gateway claims, exercising real storage, queue and model services. They bypass public JWT/OAuth/CORS verification. The genuine reconciliation job `job-f2ce0cb489044197` made four model calls and eight successful tool calls, then correctly reached `waiting_input` for missing printing proof and allocation review. Cloud financial checks included XLSX/text-PDF import, payment matching and a receipt-based correction from CAD 25 to CAD 20. A separate fresh portfolio-wide browser job job-c684f54d1e2c4114 made nine model calls/eight tool calls (seven successes and one rejected evidence reference), reached its tool budget and saved no draft. Do not conflate it with the earlier grant-scoped run. Genuine model-based bank extraction is still unverified. Full evidence and limits are in [EVALUATION.md](EVALUATION.md).

Shared-demo uses the account concurrency quota of 10, no function reservations, SQS mapping concurrency two and best-effort API rate/burst five. It provides no per-function capacity guarantee. Review capacity, permissions and cost controls before increasing quotas or adding workloads. The earlier rejected quota request made no change; no plan upgrade has been performed.

For a subsequent release, inspect the existing stacks and use an UPDATE change set. Rebuild from the allowlisted runtime when code or template changes. `scripts/configure_frontend.py --outputs FILE --site-url https://timeillusion.com/grantthread/` produces reviewed public settings; install them in `frontend/.env.production.local`, build, and run `scripts/package_cpanel.py --site-url https://timeillusion.com/grantthread/`. Keep credentials out of every `VITE_` value. `--preview` remains an explicit, separate interface-only package; it is not the currently configured release.

Follow [DEPLOYMENT.md](DEPLOYMENT.md) and [infra/README.md](../infra/README.md) for release and rollback operations. Source revision 3b4ceba is published at [g7dream/GrantThread](https://github.com/g7dream/GrantThread); public access, default branch, README and MIT licence were independently verified. Final judge handoff, video and submission details remain pending.
