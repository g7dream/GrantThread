# Demonstration script — target 4:40

**Rehearsal draft, updated 14 September 2026.** This script is not a recording or proof that its planned actions have passed. Keep the synthetic-data label visible. The final video must show a working demonstration and explain the problem, audience and value within five minutes; see [RULES_CHECK.md](RULES_CHECK.md).

## Current recording gates

- Local verification: 244 tests passed (169 backend, 50 release/build-script, 25 frontend API) and the production frontend build passed. Earlier local browser checks covered the grant workflow and financial import, conversion, adjustment and export.
- Cloud foundation: `grantthread-demo` reached `CREATE_COMPLETE` in `eu-north-1`; public API health returned 200 and an anonymous session request returned 401. Direct regional Nova Lite (`amazon.nova-lite-v1:0`) Converse tool use passed.
- Genuine application proof: job `job-f2ce0cb489044197` made four model calls/eight successful tool calls in 8.59 seconds, then correctly reached `waiting_input` for printing proof and human allocation review. Forty-nine cloud backend checks passed, including real financial imports and CAD 25→20 correction; their administrative Lambda transport bypasses JWT/OAuth/CORS/browser validation.
- Actual hosted evidence: Cognito sign-in, normal reload, persisted Bright Path data, CAD 20 actuals/CAD 250 receipts and UI-downloaded XLSX readback now pass. The fresh portfolio-wide browser job `job-c684f54d1e2c4114` made nine model calls/eight tool calls (seven successes, one evidence rejection), hit the budget and saved no draft. Do not present the earlier grant-scoped success as this fresh run's output.
- Hosted handoff: Youth Skills version 1 was shared with Northstar and one selected attachment (`invoice-insurance`). Recipient sign-in, EUR 400 package, selected original download with final JS, and source-linked question/answer/acknowledgement all passed; resolved status persisted after reload. Repaired-backend receipt-PDF values and valid JSON manifest were verified before the final JS reload, not retaken afterwards. Separate recipient shared-PDF (EUR 400) and shared-manifest downloads passed with the final JS.
- **Still pending:** three consecutive complete judge demonstrations, public video, final judge handoff and Builder ID details. Source revision 3b4ceba is publicly published and verified.

## Timed sequence

| Time | On screen | Suggested narration |
| --- | --- | --- |
| 0:00–0:20 | Bright Path portfolio, three grants, synthetic-data label | “GrantThread is for a professional managing several grants. The same activity may support two reports, but its costs, currency conversion and evidence still need a clear trail. These organisations and records are fictional.” |
| 0:20–0:50 | Show the distinct retained grant-scoped run and later portfolio-wide run, with their actual dates and results | “This earlier grant review saved an incomplete draft and asked for missing proof and human allocation review. A later portfolio run reached its tool budget and saved no draft. These are the actual recorded results. The agent cannot confirm expenses or publish a report.” |
| 0:50–1:25 | Venue proposal, invoice source, authorised 500/500 correction; printing-proof review | “Seven hundred plus five hundred euros exceeds this thousand-euro expense by two hundred. I inspect the invoice and confirm five hundred for each grant. Printing has a separate payment-proof requirement, so I review that source too. The five original expenses still total five thousand two hundred euros.” |
| 1:25–2:15 | Separate fictional CAD-reporting financial example: XLSX preview, receipt, RON 100 draft, confirmation, linked correction | “I map and review a spreadsheet in the web app. This funding receipt records four Romanian lei per Canadian dollar. The eligible receipt converts a hundred-lei expense to twenty-five Canadian dollars. Confirmation keeps the entered rate and its source. A linked twenty-lei correction retains that rate and leaves twenty Canadian dollars of reported spending.” |
| 2:15–2:35 | Supported fictional text-bank PDF preview and a payment-match draft | “This supported bank statement uses a deterministic text parser. I review a debit matched to an existing expense: it records payment without adding that expense again. This is file parsing, not a live bank feed or a demonstrated AI bank extraction.” |
| 2:35–3:15 | Financial XLSX export; return to Digital Belonging's EUR report, open its PDF, choose sharing manifest | “The financial export includes the report, ledger and funding receipts. A reviewed mapping can populate a funder's workbook. Back in the euro grant, confirmed facts produce the report. I choose its recipient, version and attachments before sharing.” |
| 3:15–3:45 | Authenticated Northstar identity, selected package, source-specific question; grantee response and acknowledgement | “Northstar receives the selected snapshot and sources. A question, response and acknowledgement stay with that report. The rest of the grantee's financial workspace is outside this package.” |
| 3:45–4:00 | Response estimates with simulated-history label | “This separate calculator illustrates how long a reply might take after the time already waited. Its history is simulated, so the range is not a prediction or a funder deadline.” |
| 4:00–4:25 | One actually executed rejected stale/unsafe case, followed by concise verification card | “Version checks stop an old screen from confirming changed financial facts. The local build passed two hundred and forty-four tests. We have not measured administrator time savings, and user validation is still pending.” |
| 4:25–4:40 | Architecture diagram showing interface, Strands/tools, AWS services and reviewed output | “Strands organises proposed work through scoped tools. People confirm the facts and decide what to share. GrantThread: many grants, one clear thread.” |

The financial CAD example is separate from the EUR 5,200 allocation scenario. Label that switch so viewers do not interpret one as a currency restatement of the other. Prepare only fictional XLSX/PDF sources. If the bank payment-match example is not exercised successfully in the recording environment, omit that segment instead of presenting a staged outcome as a new import.

## Before recording

1. Verify a fresh hosted sign-in, persistence after refresh, source access and exports for the exact frontend/backend release. Prepare a separate funder identity and test that only the selected package is visible.
2. The retained genuine execution is job `job-f2ce0cb489044197`, regional `amazon.nova-lite-v1:0` in `eu-north-1`, with four model calls, eight tool successes and 8.59-second worker duration. The already observed fresh browser run is job-c684f54d1e2c4114: nine model calls/eight tools, one evidence rejection, budget reached and no draft. If recording another new run, retain its own ID, events and duration; do not label either earlier run as fresh. Read the reason as well as the `waiting_input` label: a missing-human-input result and a tool-budget stop are different outcomes. Do not expose hidden reasoning, credentials or private AWS account details.
3. Rehearse the fictional grant and financial cases. Keep the starting state and any reset clear; a prepared successful state is not a fresh operation. The template export does not recalculate Excel formulas, so show a verified output rather than claiming general Excel compatibility.
4. Capture a real rejected stale or invalid operation. Keep the actual error visible. Do not invent an unsafe-model success or imply that one case establishes general security.
5. Show the published source revision 3b4ceba or the later verified final documentation revision. Record the final duration and public YouTube/Vimeo URL in [DEVPOST.md](DEVPOST.md). Confirm the video's public visibility independently.

The observed financial PDF/JSON downloads preceded the final JS reload; the selected original download and resolved clarification were verified afterwards. Retake any scene used as a new action in the recording rather than presenting earlier artifacts as fresh. A clearly identified functioning local demo can demonstrate implemented behavior, but do not show scripted provider responses or cached activity as a live model run. Keep any cached fallback explicitly labelled and separate from the fresh run. Disclose the original elapsed duration if model waits are shortened in editing.

The video demonstrates the supported workflow. It does not establish automatic bank extraction quality, live exchange-rate retrieval, receipt-balance consumption, spendable-cash forecasting, regulatory compliance or measured productivity gains. Record only fictional data and keep personal login details out of frame.
